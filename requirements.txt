# ASR & ML
transformers>=4.38.0
torch>=2.1.0

# Audio I/O & recording
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0

# Alignment
rapidfuzz>=3.0.0

# Text-to-speech (Windows only — macOS uses the built-in `say` command)
pywin32>=306; sys_platform == "win32"

# Data contracts
pydantic>=2.5.0

# Web dashboard
streamlit>=1.30.0
pandas>=2.0.0
altair>=5.0.0
matplotlib>=3.7.0  # optional: accuracy-trend chart in the Analytics tab

# Testing
pytest>=7.4.0
